#ifndef MAIN_H
#define MAIN_H

#include <stdio.h>

void _puts(char *s);
int _putchar(char c);
int add_numbers(int argc, char *argv[]);
int calculate_minimum_coins(int cents);
int main(int argc, char *argv[]);
#endif /* MAIN_H */
